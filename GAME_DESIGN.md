# 游戏设计文档

> 最后更新：2026-08-12（交互反馈 + 初始卡组重做 + 卡牌 UI 视觉；新增第 10 章）
> 项目路径：`D:/游戏开发/项目文件/card`
> 引擎：Godot 4.7 + GDScript
> 本文档说明：描述**设计目标**与**当前已采用的实现决策**；标有"规划/待定"的项为未来方向。

---

## 近期重大变更（2026-08-12）

- **自动攻击移除** → 改为主动**弃牌攻击**：回合中弃 1 张手牌 → 用 get_strength() 走完整攻击链结算一次攻击（一回合一次）
- **蓄力机制删除**：`store_damage` / `pending_stored_damage` 及 蓄力·蓄势 卡全部移除；`skip_attack` 一并删除
- **遗物系统新增**：规则改变效果绑定在遗物上（`data/relics.json`）；无尽 Boss 战后**遗物三选一**
  - 遗物「终末轮回」：条件式效果（①生命归零不死亡 → ②首次归零触发新规则 → ③触发过②的战斗生命可负 / 战后50%）
  - 占位遗物「头环」（可重复、无效果）保证流程
- **数值**：力量 5 开局、玩家 HP 50
- **规则**：效果文本 ↔ 代码逻辑必须一致（条件式"当X时触发Y"）
- 战斗按钮改为"普通攻击"；无法攻击时提示"你已经攻击过了。"
- **交互反馈（视觉）**：伤害/治疗/护甲飘字三色区分（红/绿/蓝）；卡牌打出改为三段式流程（飞中央 → 悬停结算 → 飞弃牌堆消散）；详见第 10 章
- **初始卡组重做**：斩击/格挡/破甲/招架 → 裂击×3/破绽×2/压制×2/铁壁×3（独立输出 + 减益利用）；新增 `damage_if_debuff` 条件伤害效果；补录数值基准（1卡=2抽=1定向检索=5伤/5防）
- **卡牌 UI 视觉**：稀有度边框+光晕 + 底部稀有度色带；类型仅文字标注（取消类型颜色区分）；稀有度四档（普通→罕见→稀有→史诗）
- **弃牌攻击简化**：选中 1 张牌即弃（去掉确认按钮）

---

## 1. 核心玩法

### 1.1 设计理念
- 回合制卡牌战斗 + 文字地图探索
- 核心循环：**车卡 → 探索世界 → 培养角色 → 死亡或毕业**
- 核心资产是**卡池**（永久积累），角色是"消耗品"
- 差异化体现在**卡牌设计逻辑**和**地图叙事探索**，而非核心系统层面

### 1.2 两套资产体系

| 资产类型 | 持续时间 | 内容 |
|---------|---------|------|
| **卡池（永久）** | 跨角色继承，不随死亡消失 | 区域/层数完成解锁的卡牌 |
| **角色（临时）** | 存活期间 | COC 式随机属性、道具/特质/属性加成 |

- 卡组完全自由搭建，随时可修改（卡组工作台已实现）
- 角色死亡 → 存档回退至最近手动存档点（角色+地图回退，卡池保留）

### 1.3 游戏循环
```
永久卡池（跨角色继承）
        ↑ 完成区域/层数解锁新卡
车新角色（COC 随机属性，待接入主流程）
        ↓
选卡组 → 进入世界（地图起点）
        ↓
探索（无尽模式：营地 → 逐层推进 / 区域探索）
  ├─ 获得属性/经验/金币
  ├─ 战斗胜利 → 层数奖励 + 解锁卡牌
  └─ 死亡 → 存档回退到最近手动存档点（卡池保留）
        ↓
角色"完全体" → 竞技场/无尽模式（无尽模式已实现）
```

### 1.4 胜利/失败条件
- 当前角色死亡（战斗中 HP ≤ 0）→ 存档回退 / 游戏结束界面
- 无尽模式：不断向北推进层数（每 10 层 Boss）
- 竞技场/更多模式：待定

---

## 2. 世界与地图

### 2.1 世界结构（设计目标）
- **一个相连的世界**（非独立选关），分为多个区域，区域通过**传送点/路径**连接
- 所有区域在一张大地图上，角色可自由来回探索
- **当前实现**：无尽模式（线性的逐层推进）+ 测试地图；多区域自由探索为待定方向

### 2.2 地图基础架构（已实现）
- **可视化节点图**（Godot 绘制，点和连线），非纯文字
- 节点稀疏型布局，每个地点有较浓的叙事内容
- 节点本身可见，但交互内容未知（标着"废弃神祠"但进去才知道有什么）
- **全局地图**（空洞骑士风格）：只有探索过的地点会写入地图，未探索区域为空白/???
- 8 方向网格布局，动态节点创建/删除
- 动画过渡（旧节点滑出淡出 + 新节点滑入淡入）
- 探索日志记录行动轨迹
- 地点内部 interactable 提供交互内容（战斗、收集、剧情等）

### 2.3 地图系统（当前架构）
- `MapController`（逻辑）↔ `MapScreen`（视图）↔ `MapState`（状态）↔ `MapDatabase`（JSON）
- **无尽模式**：营地（layer_0，休息/卡组桌）+ 向北动态生成楼层
  - 楼层类型：普通战斗 / 宝箱 / 精英 / Boss（每 10 层）/ 事件（未实装）
  - 北进需清空当前层战斗敌人；南行可回营地
- **测试地图**：营地 / 普通战斗 / 精英战斗 3 层
- 交互物类型：battle_trigger / info / heal / container / portal / deck_view / test_exp

### 2.4 地图总览（⚠️ 规划中）
- 两级视图：世界视图（区域列表）→ 区域视图（节点图）
- **当前状态**：拖动/缩放代码已实现但**可用性未验证**（BUG-3/4）
- **规划方向**：地图拖动 bug 未成功，**考虑其他优化方式**（可能移除总览弹窗，改为更简洁的呈现）

### 2.5 区域设计原则
- 每个区域独立设计，类似"副本"但处于同一世界中
- 共享世界观下的各种小故事，区域间有隐秘联系，而非线性章节
- 区域内部可以有来回探索、隐藏路线、捷径

---

## 3. 战斗系统

### 3.1 战斗模型（当前已实现）
- **弃牌攻击**：玩家回合中主动弃 1 张手牌 → 用 get_strength() 走完整攻击链结算一次攻击（一回合一次）
- 卡牌用于调节属性和操控牌堆，不是唯一伤害来源
- **无能量系统**：卡牌自身创造约束
- **游戏王模式抽牌**：开局抽 5，每回合抽 1；不自动弃手牌；不自动洗牌
- 弃牌：手牌 > 10 时点击结束回合后进入弃牌选择模式

### 3.2 伤害模型（当前设计）
- **裸属性 + buff 修正并存**：base_strength/base_dexterity（持久）+ buff 修正（叠加）
- 完整钩子链计算：`on_attack_start → calc_attack_base → calc_attack_mult → calc_attack_damage → calc_attack_final → on_attack_hit → take_damage → on_attack_end`
- 受击侧：`on_damage_taken`（易伤）、格挡抵消、`before_death`（可阻止死亡）
- **单一责任方原则**：vulnerable 只在 take_damage 处理，避免双算

### 3.3 蓄力机制（✅ 已删除 2026-08-12）
- `store_damage` / `pending_stored_damage` / 蓄力·蓄势 卡已移除；`skip_attack` 一并删除
- 相关 BUG-1 / BUG-2 随删除解决

### 3.4 状态机（已实现）
INIT → DRAW_PHASE → PLAYER_TURN → RESOLVING → ENEMY_TURN → TURN_END，终态 VICTORY/DEFEAT；非法转换被拒绝

---

## 4. 卡牌系统

### 4.1 卡池规则
- **卡牌永久解锁**：完成区域/层数获得的卡牌加入永久卡池（CardPoolManager）
- 卡池跨角色继承，即使角色死亡也不清空
- 卡组**完全自由搭建**，游戏过程中随时可修改（卡组工作台）
- 限制条件：待定

### 4.2 初始卡组（2026-08-12 重做）

| ID | 类型 | 目标 | 效果 | 稀有度 |
|----|------|------|------|--------|
| 裂击 | attack | single_enemy | 造成 5 点伤害 | common |
| 破绽 | skill | single_enemy | 施加 2 层易伤 | common |
| 压制 | attack | single_enemy | 造成 4 伤；目标有易伤则改为 8 | common |
| 铁壁 | skill | self | 获得 5 护甲 | common |

初始卡组 = 裂击×3 / 破绽×2 / 压制×2 / 铁壁×3（共 10 张）

> 设计主线：**独立输出 + 减益利用**——裂击稳定输出、铁壁防御、破绽施加易伤、压制吃易伤翻倍。
> 完整教学闭环：`破绽（上易伤）→ 压制（4→8 翻倍）`。规则突破卡（生死轮转/逆流/契约等）作为进阶解锁卡，不在开局。
> 旧的斩击/格挡/破甲/招架已移出初始卡组，其 JSON 保留待后续清理。

#### 数值基准（2026-08-12 补录）

**1 张普通卡的价值 ≈ 5**，等价换算：

| 效果类型 | 等价 1 张普通卡（价值 5） |
|---------|--------------------------|
| 伤害 / 护甲 | 5 点（1 伤 = 1 防 = 1 价值） |
| 抽牌 | 抽 2 张 |
| 定向检索 | 检索 1 张 |

- 稀有度越高，卡的总价值预算越高（common < uncommon < rare < epic），代价/条件可换取更高价值
- 示例：裂击（5 伤）、铁壁（5 甲）均为普通基准；压制（4→8 条件伤害）以「无易伤略亏、有易伤超值」体现条件卡设计

### 4.3 效果系统架构（已实现）
- **字典注册模式**：`register_effect_handler("效果名", 处理函数)`，新增效果无需改分发逻辑
- 效果类型：damage / damage_if_debuff / block / heal / 攻击力提升 / buff / 抽牌 / 检索 / 消耗 / 洗牌等
- 支持 `base_stat`（基于力量/敏捷计算效果值）与 `scaling_key`（描述占位符）
- 标签系统：分类（type/keyword/archetype/rarity）+ 卡牌标签检索
- **钩子调用规范**：所有钩子名统一收敛到 `scripts/effects/hook_registry.gd`（常量 + 清单），
  复用旧效果遵循"调用钩子 → 实现效果"流程，完整规范见 `EFFECT_HOOKS.md`

### 4.4 卡牌升级
- upgrade JSON 已定义、GameData.upgrade_card_at_index() 已实现
- ⚠️ 游戏内无触发升级的入口（规划在 reward 界面或地图交互物加入）

### 4.5 规划方向
- **卡牌重新设计**（围绕"规则突破 + 减益利用"；数值基准：1 卡≈5 价值）
- 遗物获取（Boss 三选一）已实现；遗物种类扩充

---

## 5. 角色系统

### 5.1 角色创建（待接入主流程）
- 参考 COC（克苏鲁的呼唤）桌游车卡形式
- 随机属性，强调"优势与弱势的组合"而非纯强度随机
- **当前实现**：CharacterData（base_stats/current_stats/特质/物品）+ CharacterManager（user://characters.json 持久化）
- ⚠️ **主流程未接入角色创建**（新游戏直接进无尽地图）

### 5.2 角色特质（待定）
- 可能影响战斗、地图交互、事件选项
- CharacterData.traits 已预留字段

### 5.3 角色成长（已实现部分）
- 通过探索获取：经验升级（等级/属性点）、金币、卡牌
- 地图状态面板支持属性点加点（力量/敏捷/生命）
- 死亡后存档回退到最近手动存档点

---

## 6. 存储规则

| 项目 | 生命周期 | 说明 |
|------|---------|------|
| 永久卡池 | ∞ | user://card_pool.json，解锁卡牌永不丢失 |
| 角色数据 | 角色存活期间 | user://characters.json，死亡回退到手动存档点 |
| 地图进度 | 与角色绑定 | user://savegame.json 的 map_state，死亡回退 |
| 游戏进度 | 当前 run | user://savegame.json（version 2，GameProgress 枚举） |

- 战斗前自动存档（save_before_battle），战斗后恢复并清理已死亡敌人
- 死亡惩罚 = 存档回退（角色+地图回退，卡池保留）✅ 已定案

---

## 7. UI/UX 规范

### 7.1 命名约定
- 全部使用 snake_case；GDScript 脚本；数据文件可用中文命名

### 7.2 手牌布局
- HandArea: offset_left=192, anchor_right=1
- CARD_WIDTH = 140.0；动态 spacing = min(ideal, max_spacing)

### 7.3 交互原则
- 非指向性卡：拖出手牌区打出
- 指向性卡：拖拽移到中央 + 箭头选目标；短按进目标模式
- 选择模式（弃牌）：顶部选择栏；弃牌攻击选中 1 张即弃（无确认按钮）；回合末弃牌阶段选多张后确认/跳过

### 7.4 卡牌视觉（2026-08-12 改进）
- 卡面：统一深色底（`CARD_BG_COLOR`）+ 稀有度边框色（3px）+ 稀有度色光晕（shadow）
- 底部**稀有度色带** `RarityBand`（10px）+ 角标文字 `RarityLabel`（白字）
- 类型仅以文字标注（`TypeLabel` 中性灰），不再用颜色区分类型（避免与稀有度颜色混淆）

| 稀有度 | 边框/色带颜色 | 角标文字 |
|--------|--------------|---------|
| common | 灰白 (0.72,0.72,0.75) | 普通 |
| uncommon | 绿 (0.3,0.8,0.5) | 罕见 |
| rare | 蓝 (0.35,0.6,1.0) | 稀有 |
| epic | 紫 (0.7,0.4,0.95) | 史诗 |

实现：`card_ui.gd` 的 `_set_card_color` / `_set_rarity` / `_get_rarity_color` / `_get_rarity_text`
> 类型小图标（原 `Icon` 节点）与顶部类型色带（`TypeBand`）已移除；稀有度为四档（无 `basic`）

---

## 8. 待定与规划

### 8.1 已定案
- [x] 死亡惩罚：存档回退（角色+地图回退，卡池保留）
- [x] 效果系统：字典注册模式（可扩展）
- [x] 伤害模型：裸属性 + buff 修正 + 完整钩子链
- [x] 无尽模式：作为当前主要玩法
- [x] 2026-08-05 死代码剪枝完成

### 8.2 规划方向（2026-08-05 确认，2026-08-12 更新）
- [x] ~~**蓄力**~~：已删除（2026-08-12）
- [ ] **地图总览**：拖动 bug 未成功，考虑其他优化方式
- [ ] **卡牌效果大修**（数值、机制、新卡）
- [ ] 角色创建系统接入主流程（COC 式随机属性）
- [ ] 卡牌升级触发机制
- [ ] buff stack_decay 逐个配置（衰减规则）
- [ ] 无尽模式 EVENT 事件实装
- [ ] 敌人 AI 扩展（当前仅 basic 随机）

### 8.3 待讨论
- 角色特质系统
- 地图快捷传送/回城机制
- 区域间隐秘联系设计
- 竞技场/更多模式
- 卡牌限制规则
- 故事线设计（聊斋式多结局，跑团式自由选择）

---

## 9. 已知问题摘要

| Bug | 状态 | 规划 |
|-----|------|------|
| BUG-1 蓄力后伤害实时显示 | ✅ 关闭（2026-08-12） | 蓄力已删除 |
| BUG-2 多次蓄力重复加伤害 | ✅ 关闭（2026-08-12） | 蓄力已删除 |
| BUG-3 地图总览拖动 | 代码已实现，可用性未验证 | 地图其他优化 |
| BUG-4 地图总览缩放 | 代码已实现，可用性未验证 | 地图其他优化 |
| BUG-5 fit_zoom 启发式 | 低优先级 | 随地图优化处理 |

> 详细实现见 PROJECT_STATE.md 与知识库 /memories/repo/

---

## 10. 交互反馈设计（2026-08-12 新增）

> 本轮范围：**纯视觉反馈**（音效后续单独规划）。目标：让玩家「看得懂发生了什么」并「感受到操作结果」。

### 10.1 设计原则

- 最小改动：复用现有 tween 体系，不引入粒子 / shader / 新场景结构
- 反馈分层：数值结果（飘字）优先于过程演出（动画），二者重合并存
- 克制原则：敌人被动产生的 buff / 治疗不弹字，仅玩家**卡牌效果**弹字，避免战场信息过载

### 10.2 伤害 / 治疗 / 护甲飘字（已实现）

统一入口 `UIController.show_damage_number(target, amount, kind)`，`kind` 决定颜色与符号：

| kind | 语义 | 颜色 | 符号 |
|------|------|------|------|
| `damage`（默认） | 普通伤害 | 红 `Color.RED` | `-%d` |
| `heal` | 治疗回复 | 绿 `(0.35, 0.9, 0.45)` | `+%d` |
| `block` | 获得护甲 | 蓝 `(0.45, 0.75, 1.0)` | `+%d` |

- 敌人侧渲染：`EnemyUI.show_damage_number(amount, kind)`（同一颜色约定）
- 信号源：`effect_resolver.healing_done → "heal"`、`block_gained → "block"`（`BattleController` 连接）；
  普通伤害仍走既有 `enemy_system.enemy_damaged` / `_perform_attack`（默认红）
- 约束：`amount <= 0` 不弹字；**反击不区分颜色**（保持红）

### 10.3 卡牌打出流程（已实现）

原「飞到目标 + 淡出」改为**三段式**，让玩家看清自己打出了什么牌、牌去了哪里：

```
手牌位置 → ① 飞向屏幕中央(0.2s) → ② 悬停结算(0.3s，RESOLVING 状态展示) → ③ 飞向弃牌堆(0.2s) 淡出消散
                                          └ 效果在此同步结算（弃牌堆计数即时 +1）
```

- `CardUI.fly_to_center()` / `fly_to_discard(target_center)`：两段 tween（await 风格）
- `BattleController.play_card_with_animation()`：编排三段时序，`await play_card()` 期间卡牌悬停中央
- `UIController.detach_card_node()` / `reattach_card_node()`：打出前把节点从手牌映射摘出（**不释放**），
  避免 `hand_changed` 重排时 `queue_free` 打断动画；打出失败时 reattach + 复位
- `UIController.get_discard_pile_global_pos()`：读取 `DeckInfo/DiscardPile` 作为飞行终点
- 健壮性：`CardUI.is_playing_animation` 标志在飞行期间置 `true`，阻止悬停放大动画与飞行 scale 竞争

### 10.4 边界与后续优化（记录）

| 项 | 现状 | 说明 / 方向 |
|----|------|------------|
| 悬停时长 | 0.3s（= RESOLVING 等待） | 可接受；如需更久可在 `play_card` 返回后再补等待，不推荐改状态机节奏 |
| 弃牌堆计数时序 | 逻辑即时 +1，视觉约 0.5s 后到位 | 保持逻辑优先；严格「消失后同步」需延迟刷新（暂不做，避免侵入同步结算） |
| Exhaust 消耗卡终点 | 统一飞弃牌堆位置 | 当前 5 卡均无 Exhaust 标签、`DeckInfo` 无消耗堆显示；未来需单独定义终点 |
| buff tick / 敌人意图 | 不弹字（`_apply_tick_effect`、敌人 defend/buff 直接走单位方法） | 克制原则，保持战场信息不过载 |
| 死代码 `player_ui.gd` | 未被 `BattleScene.tscn` 引用（PlayerArea 无脚本） | 本次未清理，待后续剪枝 |

### 10.5 已否决方案（记录备查）

- 卡牌打出瞬间「扩散光环」——用户否决（改为三段式）
- 反击伤害单独配色——用户否决（保持红）
- 音效系统——本轮纯视觉，不纳入（后续单独规划）
