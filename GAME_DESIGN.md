# 游戏设计文档

> 最后更新：2026-05-31（添加地图总览功能设计）
> 项目路径：`D:/游戏开发/项目文件/card`
> 引擎：Godot 4.6 + GDScript

---

## 1. 核心玩法

### 1.1 设计理念
- 回合制卡牌战斗 + 文字地图探索
- 核心循环：**车卡 → 探索世界 → 培养角色 → 死亡或毕业**
- 核心资产是**卡池**（永久积累），角色是"消耗品"
- 差异化体现在**卡牌设计逻辑**和**地图叙事探索**，而非核心系统层面

### 1.2 两套资产体系

| 资产类型 | 持续时间 | 内容 |
|---------|---------|------|
| **卡池（永久）** | 跨角色继承，不随死亡消失 | 区域完成奖励的卡牌 |
| **角色（临时）** | 存活期间 | COC式随机属性、探索获得的道具/特质/属性加成 |

- 卡组完全自由搭建，随时可修改
- 角色死亡 → 存档回退至最近手动存档点（角色+地图回退，卡池保留）

### 1.3 游戏循环
```
永久卡池（跨角色继承）
        ↑ 完成区域解锁新卡
车新角色（COC随机属性）
        ↓
选卡组 → 进入世界（地图起点）
        ↓
探索各区域（叙事+探索+战斗）
  ├─ 获得属性/道具/特质
  ├─ 探索新区域 → 解锁永久卡牌
  └─ 死亡 → 存档回退到最近手动存档点（角色+地图回退，卡池保留）
        ↓
角色"完全体" → 竞技场/无尽模式（待定）
```

### 1.4 胜利/失败条件
- 当前角色死亡（战斗中 HP ≤ 0）→ 存档回退
- 无传统"通关"条件，目标是培养角色探索世界
- 竞技场/无尽模式：待定

---

## 2. 世界与地图

### 2.1 世界结构
- **一个相连的世界**（非独立选关）
- 分为多个区域，区域通过**传送点/路径**连接
- 角色可通过道具或花费货币**回城**（home base）或**回到上个传送点**
- 所有区域在一张大地图上，角色可自由来回探索

### 2.2 地图基础架构
- **可视化节点图**（Godot 绘制，点和连线），非纯文字
- 节点稀疏型布局，每个地点有较浓的叙事内容
- 节点本身可见，但交互内容未知（标着"废弃神祠"但进去才知道有什么）
- **全局地图**（空洞骑士风格）：
  - 只有探索过的地点会写入地图
  - 未探索区域在地图上为空白/???
  - 地图随探索逐步补全
- 8 方向网格布局，动态节点创建/删除
- 动画过渡（旧节点滑出淡出 + 新节点滑入淡入）
- 探索日志记录行动轨迹
- 地点内部的 interactable 提供交互内容（战斗、收集、剧情等）

### 2.3 探索体验
- **奖励驱动开始**：听说某区域有某张卡 → 为了拿卡去探索
- **剧情驱动继续**：进入后被发现的故事吸引，继续推进
- **地图自由度来自有向图结构**：节点可随意连接，支持：
  - 捷径/近路
  - 迷宫式结构
  - "瞬移"式传送
  - 单向通路
- 文字地图移动轻松，可以设计高密度节点和回头路探索

### 2.4 区域设计原则
- 每个区域独立设计，类似"副本"但处于同一世界中
- 共享世界观下的各种小故事，区域间有隐秘联系，而非线性章节
- 区域内部可以有来回探索、隐藏路线、捷径

### 2.5 地图总览（全局世界地图弹窗）
- 点击header"地图"按钮弹出 PopupPanel 窗口（700×520）
- **两级视图**：
  - 世界视图：显示所有区域列表，每个区域可点击"进入"
  - 区域视图：显示该区域内所有节点（已访问/未访问/当前位置），支持拖动和缩放
- **适配原则**：自动缩放适配视口，确保不同大小的区域地图都能显示完整
- **视觉风格**：深色背景+文字节点（ColorRect+Label），Line2D绘制连线
- **拖动**：鼠标左键拖拽平移地图（MOUSE_BUTTON_LEFT + MOUSE_FILTER_STOP + gui_input）
- **缩放**：鼠标滚轮连续缩放（canvas.scale 变换，非重建画布），范围 [0.25, 4.0]
  - 缩放保持视口中心稳定：`canvas.position = center - map_local * new_scale`
- **滚动条**：隐藏（SCROLL_MODE_SHOW_NEVER）
- **地区数据定义**：在JSON地图的 "regions" 字段中定义区域归属

> ⚠️ 当前状态：拖动和缩放功能尚未实现（2026-05-31）

---

## 3. 战斗系统

### 3.1 ⚠️ 待重构
> 当前战斗系统与卡牌逻辑由其他 AI 生成，存在设计偏差：
> - 卡牌数值和战斗逻辑与设计意图不一致
> - 角色属性与战斗属性的关系需重新设计
> - **卡牌和战斗系统需要重构**

### 3.2 基础框架（待定）
- 回合制
- 无传统能量系统，卡牌自身创造约束
- 详情的战斗设计待卡牌系统重构时讨论

---

## 4. 卡牌系统

### 4.1 卡池规则
- **卡牌永久解锁**：区域完成后获得的卡牌加入永久卡池
- 卡池跨角色继承，即使角色死亡也不清空
- 卡组**完全自由搭建**，游戏过程中随时可修改
- 限制条件：待定

### 4.2 当前卡牌数据（待重构）
| ID | 类型 | 目标 | 效果 | 稀有度 |
|----|------|------|------|--------|
| 斩击 | attack | self | temp_damage_boost +5 | basic |
| 格挡 | skill | self | block 5 | basic |
| 蓄力 | skill | self | skip_attack + store_damage | uncommon |
| 蓄势 | skill | self | skip_attack + draw 2 | uncommon |
| 破甲 | attack | self | ignore_block + temp_damage_boost +3 | uncommon |
| 招架 | skill | self | counter_stance + block 5 | uncommon |
| 致弱 | skill | single_enemy | apply_buff vulnerable 2层 | common |

初始卡组：斩击 × 3, 格挡 × 3, 蓄力 × 2, 蓄势 × 2

---

## 5. 角色系统

### 5.1 角色创建（待定）
- 参考 COC（克苏鲁的呼唤）桌游车卡形式
- 随机属性，强调"优势与弱势的组合"而非纯强度随机
- 具体规则待设计

### 5.2 角色特质（待定）
- 可能影响战斗、地图交互、事件选项
- 具体设计待讨论

### 5.3 角色成长
- 通过探索地图获取：属性提升、道具、特质、临时卡牌
- 死亡后存档回退，角色回到最近手动存档时的状态

---

## 6. 存储规则

| 项目 | 生命周期 | 说明 |
|------|---------|------|
| 永久卡池 | ∞ | 完成区域解锁，永不丢失 |
| 角色数据 | 角色存活期间 | 死亡回退到手动存档点 |
| 地图进度 | 角色存活期间 | 与角色绑定，死亡回退到手动存档点 |
| 区域解锁状态 | 与角色绑定 | 死亡回退到手动存档点 |

---

## 7. UI/UX 规范

### 7.1 命名约定
- 全部使用 snake_case
- GDScript 脚本

### 7.2 手牌布局
- HandArea: offset_left=192, anchor_right=1
- CARD_WIDTH = 140.0

---

## 8. 待定事项

### 8.1 待讨论
- [ ] 角色创建系统（COC 式随机属性）
- [ ] 角色特质系统
- [ ] 地图快捷传送/回城机制
- [ ] 区域间隐秘联系设计
- [ ] 竞技场/无尽模式
- [ ] 卡牌限制规则
- [ ] 故事线设计（聊斋式多结局，跑团式自由选择）
- [x] 死亡惩罚定案：存档回退（死亡回到最近手动存档点，角色+地图回退，卡池保留）

### 8.2 已知问题
- [ ] 当前战斗/卡牌代码需重构
- [ ] 角色创建未实现
- [ ] 地图系统缺少条件连接/捷径系统
- [ ] 存储系统需适配新规则（卡池≠角色存档）
